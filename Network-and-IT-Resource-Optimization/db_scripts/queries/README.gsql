CREATE QUERY README() FOR GRAPH Storage {
    PRINT "Network and IT resource graph for modeling and analyzing"; 
    PRINT "the impact of the hardware outage on workloads.";

    PRINT "The queries do not need to be run in a specific order.";
}
